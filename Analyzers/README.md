# Shipped analyzer

`MemoryToolkit.Analyzers.dll` is labelled `RoslynAnalyzer`, so Unity loads it automatically —
no setup. See `docs/ANALYZER.md` for the rules, the `.editorconfig` knobs, and how to rebuild
from `Analyzers~/`.
